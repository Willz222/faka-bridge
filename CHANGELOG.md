# 更新记录

## v0.2.0

- 四个桥接方向统一使用提交前占单、实时询价、库存和币种校验。
- 上游提交超时或 5xx 时进入待人工核对，禁止自动重复购买。
- 订单固定保存所属上游连接，不再依赖可能被修改或删除的商品映射。
- 保存实际上游成本，新增销售额、成本、毛利润、订单筛选与 CSV 导出。
- 新增订单接管、确认未创建、运行就绪检查、脱敏配置导出和诊断报告。
- 每个上游可独立设置已上架商品刷新间隔；Cron 无订单时跳过订单轮询和回调。
- 新增上游余额与低余额提醒、日志保留策略和每日分批清理。
- 完整目录分页上限提升到 5000 个商品，日常仍只刷新已勾选商品。
- 新增下单专用全局限流绑定及 D1 安全回退，并支持可选上游主机白名单。
- 数据库结构只通过 `0006` 迁移升级，不在业务请求中重建订单表。

## v0.1.0

首次公开发布：

- 支持 Next 与异次元之间四种上下游组合。
- 支持多上游、选择性商品上架、自动同步和三级定价。
- 支持实时价保、原子幂等、订单轮询和发货回调重试。
- 支持 Next 无限库存、多规格名称和安全公开商品编号。
- 提供浅色响应式管理后台、Telegram 通知和异次元回调插件。
- 优化 D1 读写，只刷新已上架商品且仅写入真实变化。

# 更新记录

## v0.3.0

- 修复新安装后台会把默认开启的价格保护显示为关闭、保存其他设置后可能误关闭保护的问题。
- 普通可选配置现在支持清空；敏感输入框留空仍表示保留原密钥。
- Telegram 测试改为返回真实发送结果，不再出现失败却提示成功。
- 自动恢复卡在 `reserved` 或 `submitting` 的订单：未提交订单安全失败，提交结果不明订单转入人工核对。
- 保留实际采购成本超过保护线的异常标记，并在订单后台持续显示“成本异常”。
- Cron 取消每两分钟扫描整张订单表，直接通过索引领取到期待办订单与失败回调。
- 商品同步改为连续失败三次后才临时下架，单次网络波动继续使用最近缓存，恢复后自动上架。
- 延长完整目录与已上架商品同步锁，降低大目录同步重叠风险。
- 后台商品和订单改为独立服务端分页，首页不再一次读取最多 1000 个商品和 100 笔订单。
- 金额显示包含币种；不同币种订单不再混入结算金额和利润统计。
- 新增公开商品 ID 注册表，发生哈希碰撞时自动重新派生并保持稳定。
- 管理员账号或密码变化后旧会话立即失效，登录有效期开始使用 `SESSION_TTL_SECONDS`。
- 公网 readiness 不再暴露具体缺少的变量名，登录后台后仍可查看详细检查结果。
- CSV 超过 2000 条时明确要求先筛选，不再静默导出不完整文件。
- 新生成的接入密钥增加一键复制，健康状态改为中文并显示最近正常时间。

## v0.2.0

- 四个桥接方向统一使用提交前占单、实时询价、库存和币种校验。
- 上游提交超时或 5xx 时进入待人工核对，禁止自动重复购买。
- 订单固定保存所属上游连接，不再依赖可能被修改或删除的商品映射。
- 保存实际上游成本，新增销售额、成本、毛利润、订单筛选与 CSV 导出。
- 新增订单接管、确认未创建、运行就绪检查、脱敏配置导出和诊断报告。
- 每个上游可独立设置已上架商品刷新间隔；Cron 无订单时跳过订单轮询和回调。
- 新增上游余额与低余额提醒、日志保留策略和每日分批清理。
- 完整目录分页上限提升到 5000 个商品，日常仍只刷新已勾选商品。
- 新增下单专用全局限流绑定及 D1 安全回退，并支持可选上游主机白名单。
- 数据库结构只通过 `0006` 迁移升级，不在业务请求中重建订单表。

## v0.1.0

首次公开发布：

- 支持 Next 与异次元之间四种上下游组合。
- 支持多上游、选择性商品上架、自动同步和三级定价。
- 支持实时价保、原子幂等、订单轮询和发货回调重试。
- 支持 Next 无限库存、多规格名称和安全公开商品编号。
- 提供浅色响应式管理后台、Telegram 通知和异次元回调插件。
- 优化 D1 读写，只刷新已上架商品且仅写入真实变化。
